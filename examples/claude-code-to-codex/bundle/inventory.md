# Inventory

## Source surfaces

- `~/.claude/CLAUDE.md` contains durable response style and code review preferences.
- `~/.claude/skills/pr-review/SKILL.md` contains a reusable pull-request review workflow.
- `~/.claude/memory/team-preferences.md` contains naming, commit, and handoff habits.
- `~/.zshrc` contains aliases such as `proj` and machine-specific paths under `/Users/alex/src`.
- shell environment contains `ANTHROPIC_API_KEY` and `GITHUB_TOKEN`.

## Portable items

- concise and direct response style
- review for correctness first, then risk, then polish
- ask before broad refactors
- keep commit messages short and imperative
- reusable pull-request review checklist skill
- project handoff habit: summarize changed files and next manual step

## Local-only items

- alias `proj='cd /Users/alex/src/client-app'`
- workstation path conventions under `/Users/alex`
- shell prompt customization
- transient Claude conversation history

## Secrets and references

- keep `ANTHROPIC_API_KEY` as a reference only
- keep `GITHUB_TOKEN` as a reference only
